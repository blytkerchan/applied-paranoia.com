# AWS credentials and region
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=us-east-1

# Optional AWS CLI profile name
AWS_PROFILE=

# Terraform backend state location
AWS_STATE_BUCKET=
AWS_STATE_KEY=applied-paranoia/terraform.tfstate

# Deployment settings
DEPLOYMENT_STAGE=prod

# Optional Terraform overrides
ROOT_DOMAIN=applied-paranoia.com

# Required for CloudFront custom domain TLS (must be in us-east-1)
ACM_CERTIFICATE_ARN=
