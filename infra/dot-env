# Optional. bootstrap works without this file entirely, as long as an AWS
# session is already active (aws sso login / aws configure / an
# OIDC-assumed role's exported credentials). Only needed to pin which AWS
# CLI profile bootstrap and Terraform should use.
#
# Copy this file to .env in this directory if you need it: cp dot-env .env

# Optional AWS CLI profile name
# AWS_PROFILE=your-profile
